这一章演示更多排版细节。

正文使用衬线字体，行距较宽，专为长时间阅读优化。顶栏的 A− 和 A+ 可以调节字号，调好的字号同样记在 localStorage 里，下次自动沿用。

右上角的「目录」按钮会滑出章节列表，当前章节高亮显示。在桌面端，你也可以直接用键盘左右方向键翻章。

* * *

关于深浅色：默认跟随你的系统设置。点顶栏的 ◐ 可以手动切换，切换后的选择会被记住，优先于系统设置。

这套界面刻意保持克制——温暖的中性配色、充足的留白、一个赤陶色作为唯一强调色。没有引入任何外部字体或脚本库，所有代码都在本项目里，可以完全离线、自托管运行。
